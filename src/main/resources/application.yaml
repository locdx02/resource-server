spring:
  application:
    name: resource-service
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:9999/realms/dev-realm
server:
  port: 8886
  servlet:
    context-path: /resource-service

